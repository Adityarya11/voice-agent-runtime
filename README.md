# Voice calling runtime

serves differently for your different puproses.

to get started drop the prompt and the tools you need for the orchestration use the inbuilt/predefined set of tools like google calendar(placeholder we shall think about it later) and see the blazing fast, concurrent AI caller handling all your client/customers at ease.
